// Deprecated: Teacher AttendanceRoster is disabled. Use manager/AttendanceRoster instead.
import React from 'react';
export default function AttendanceRoster() {
  if (typeof window !== 'undefined') {
    // eslint-disable-next-line no-console
    console.warn('Teacher AttendanceRoster is under-construction.');
  }
  return null;
}
